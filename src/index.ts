// This app is deployed as a standalone SPA (not an npm library).
// Clients integrate by embedding via <iframe> with URL query parameters.
// See README.md for integration instructions.

export { SprintCheckSDK } from './components/sdk/SprintCheckSDK';
export type { SprintCheckSDKProps } from './components/sdk/SprintCheckSDK';
